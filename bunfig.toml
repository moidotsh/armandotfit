[install]
# Use exact versions
exact = true
production = true
optional = true

[install.cache]
# Enable the global cache
enabled = true

# JavaScript runtime options
[runtime]
env = { NODE_ENV = "production" }
