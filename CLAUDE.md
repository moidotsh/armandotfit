# armandotfit

> armandotfit is a personal fitness PWA — the AM/PM hypertrophy program, coarse exercise identities, tag-based realization context, and computed-at-read progression — built on [arqavellum](../arqavellum) (the public PWA-first starter shell). Theme: **THE BROADSHEET — the daily training paper: the headline law, agate figures, the record red, the wire** (see `docs/architecture/broadsheet-thesis.md`).

This file is the repo-level operating context for Claude Code sessions at the armandotfit root. It auto-loads. Read the relevant section before landing any armandotfit change.

**The greenslate rebuild (2026-10)** replaced the prior Phase 1–6 schema (program templates, user plans, eligibility, capability inventory, presets, trigger-maintained analytics — 34 tables) with a five-table logging core. That machinery is deleted, not dormant; git history preserves it. Docs describing it are superseded by this file.

## Invariants

Load-bearing rules that aren't obvious from the code:

1. **Bun only.** Never commit `package-lock.json` or `yarn.lock`. `audit-pattern-compliance.ts` (S19) enforces this.

2. **PWA-first, two-layer delivery.** Web static export is the supported default; native is a consumer extension (scaffolding + `assets/*` PNGs ship; a native release still needs real bundle IDs + platform validation). PWA tags + id'd styles reach the deployed HTML twice: `scripts/inject-critical-web.ts` (chained into `build:web`/`vercel-build`, copies them into every exported route) and the runtime injection in `app/_layout.tsx` (covers dev + anything the export strip misses). `index.html` is the declaration both mirror — keep the three in sync. The **ink boot plate** (id'd `<style>` in `index.html`, lifted by `markBootReady()`) is part of this set. Barrel shims (`shims/`, regenerated by `scripts/sync-barrel-shims.ts` before every expo command, enforced by `audit-shim-sync.ts`) keep icon/date-fns imports from pulling whole libraries; production exports stub `app/dev/*` (gated on `EXPO_PUBLIC_DEV_SURFACES`, set by `start`/`web`). Run `bun run verify:web-build` after touching export plumbing.

3. **THE BROADSHEET theme — light default, dark opt-in, one dialect declaration, the wire.** `constants/theme.ts` sets `DIALECT = 'ink'` (flat atmosphere, chit toasts, curtain transitions — the drawer primitive stays synced but unwired: THE BROADSHEET has NO tab bar; the Desk is a stack from home's jump lines, and the **ticker** (`components/composed/SessionStrip.tsx`, wire plate) pins under every Desk header while a session runs — one tap returns to the Floor). Two palettes: **Paper** (light, warm cream `#F6F3EB` + warm ink) and **Evening Edition** (dark, warm black `#14110C` + cream text) — both WARM, never cool gray; the live **Floor follows the mode** (the register difference is density + scale, not a second palette), and `colors.focus.*` is **the wire**: mode-independent warm black, for the chit, the curtain, and the ticker only. One RECORD red: brand fill `#C24100` light / `#FF7E45` dark, with measured companions (`brandText`, `textOnBrand` — see broadsheet-thesis §3.3 matrix); it appears exactly three ways (record mark, primary verb, living pulse) — never decorates. **The headline law:** exactly one element per screen at display scale, second voice ≥1.4× quieter — probe-enforced (`.shots/vision2/`, not committed). Type: **Rokkitt** (statements — one variable woff2, slab serif, 29 KB) + **Azeret Mono** (agate + every changing/aligned figure — mono is tabular by construction; the `mobileCounter` token is the mono face's one display size, ported to the shell) + platform sans for body; shapes square-cut print (2/4/2/2/2). THE COUNT's tally glyph is retired from all screens (`TallyStrip` stays shell-synced in the kit, unwired). `audit-ui-theme.ts` (S7) bans hardcoded hex. Icon surfaces (`public/icons/*`, `assets/*`) are ember/ink barbell marks — regenerate, don't hand-edit.

4. **Five tables exactly.** `users` (one row per lifter: display name, `weight_unit`, `rest_days` — picker config), `exercises` (coarse identity: `user_id NULL` = system seed row, name is the natural key), `sessions` (`started_at`, `split_day` nullable, note; AM and PM are two rows), `logged_exercises` (`tags text[]` — the ONLY context mechanism), `logged_sets` (`reps` + `weight` + note — **a row is a completed set**; no completion flags, no target columns). Adding a sixth table or a context column is a design decision, not a convenience — take it to the owner first. The migration pair `supabase/migrations/20261001000000_greenslate_rebuild.sql` (+ `20261001000001_grants.sql`, the role-grant companion — tables without grants are unreachable past Supabase's schema privileges) is the canonical schema (drop-and-recreate; applying to the live project is approval-gated).

5. **Computed, never stored.** Streaks, PRs, weekly buckets, day-of-split suggestions — all derived at read time from raw sessions (`progressionService`, `analyticsService`, `getNextSplitDay`). No triggers, no aggregate tables, no cached progression values anywhere. If a read is slow, index the raw tables; don't precompute.

6. **History is immutable raw fact.** No rewrites of `sessions`/`logged_exercises`/`logged_sets` (adding a NEW nullable column in a migration is fine). The only FK from history is `logged_exercises.exercise_id → exercises` (RESTRICT — identity is stable). Vocabulary lives in TS, never in CHECK constraints — the DB stores TEXT/tags so the vocabulary extends without migrations.

7. **Exercise identity is coarse; realization is tags.** A separate identity exists only when the distinction passes the three-question test: (a) decided before the gym, (b) named differently without thinking, (c) mixing trajectories would mislead. Everything else — grip, attachment, machine, stance, execution style — is a tag on `logged_exercises`. Suggested tags ride the program slots (`SplitSlot.suggestedTags`) and pre-fill the draft; free-form tags autocomplete from `TAG_VOCABULARY_SEED` + the user's own history. **Promotion rule:** a tag becomes a column/registry only after (a) ≥10 consistent uses AND (b) the same filter/comparison was attempted twice and free-text couldn't answer it.

8. **The program is TypeScript, never the database.** `shared/exercises/splits.ts` holds the AM/PM split (4 days × AM/PM × 4 slots + the one-a-day compression), each slot carrying coarse exercise key, suggested tags, and the programmed Rx (`sets [min,max]`, `reps [min,max]`). `shared/exercises/data.ts` is the sole display source (42 entries: 26 split identities + 16 browsable extras; identity joins by NAME — entries sharing a name are the same exercise row). Session start hydrates the draft locally (`hydrateFromSplit` — no DB round-trip); save resolves names find-or-create. Adding a split exercise = `splits.ts` (ExerciseKey + slot) + `data.ts` entry + optional seed INSERT in the same change.

9. **Audit scripts are canonical.** If this file and `scripts/audit-*.ts` disagree, the scripts win.

10. **The 490px height-budget test** carries over. Any new screen fits iPhone SE compact without scrolling for the primary action. See `docs/architecture/mobile-premium-design-system.md`.

11. **Auth is email/password and the guard is ON.** `APP_LAYOUT.authGuard = true` in `constants/layout.ts` — every route walls except the auth screens (this app owns real per-user history; the starter's open posture does not apply). No PIN primitives.

## Pre-commit checks (read before committing)

Armandotfit has 13 structural audits + `tsc --noEmit` + structural ESLint on every `git commit` via `.husky/pre-commit`. Run them on the working tree **before** staging:

    cd armandotfit && bun run lint:structure && bunx tsc --noEmit

### The two universal escape hatches

- **`// <check>-exempt`** — suppresses one violation within a 300-char lookback. Use sparingly with a justification.
- **`git commit --no-verify`** — skips the hook entirely. Genuine emergencies only.

### The 13 audits, in pre-commit order

| # | Script | Codes | Catches |
|---|--------|-------|---------|
| 1 | `audit-barrels.ts` | `[S5-internal]`, `[S5-external]` | Own-barrel imports (circular); direct-path imports when a barrel re-exports the symbol. Only audit with `--fix`. |
| 2 | `audit-data-layer.ts` | `[S9-import]`, `[S9-call]`, `[S13]`, `[D5]` | Direct `supabase.*` in `app/`/`hooks/`/`components/`/`context/`; inline `queryKey`; repositories not returning `RepositoryResult<T>`. |
| 3 | `audit-state.ts` | `[D3]`, `[D10]` | `useMutation` not touching a cache primitive; Zustand stores missing the 5 `// SECTION:` markers. |
| 4 | `audit-security.ts` | `[S12]`, `[SE2]`, `[S10]` | Anchored regex `.test()` in client code; AsyncStorage outside the allowlist; `Alert.alert` with raw `error.message`. |
| 5 | `audit-logging-errors.ts` | `[S11]`, `[S10]` | Live `console.*`; raw `throw new Error(...)` outside the carve-out. |
| 6 | `audit-ui-theme.ts` | `[S7]`, `[C3]` | Hardcoded hex colors; `Dimensions.get('window'/'screen')`. **Critical for the ember palette.** |
| 7 | `audit-component-quality.ts` | `[C1]`, `[C2]`, `[C4]` | Direct `router.push/replace/back`; RN `Modal`; `ActivityIndicator` outside loading primitives. |
| 8 | `audit-testing-types.ts` | `[D6]`, `[T1]`, `[T2]` | UI code importing raw `shared/types`; test files outside `__tests__/`; inline `vi.mock()`. |
| 9 | `audit-pattern-compliance.ts` | `[S19]`, `[C10]` | `package-lock.json`/`yarn.lock` in tree; imports of deprecated symbols. |
| 10 | `audit-runtime-resilience.ts` | `[R4a]`, `[R4b]`, `[R1]` | `setInterval` without `clearInterval`; unpaired `addEventListener`; unguarded async `useEffect` setState/navigate. |
| 11 | `audit-screen-body.ts` | `[SB1]` | Full-screen route missing the body-width policy. Suppress with `// sb1-exempt`. |
| 12 | `audit-mobile-content-width.ts` | `[SB2-portal]`, `[SB2-magic-number]`, `[SB2-surface]` | Portal panels missing the width spread; literal `maxWidth` under `components/`. |
| 13 | `audit-shim-sync.ts` | `[shim-sync]`, `[redirect]`, `[dead-shim]` | A shimmed-barrel import the shim doesn't re-export (silent runtime `undefined`); missing metro redirects; stale shim entries. |

Structural ESLint adds `[S6]` (`{expr && <Component/>}` render leak) and `[S8]` (raw `fetch()`).

### The five that bite most often

1. **S5 barrels** — same-folder imports go relative; cross-folder go through the barrel. `bun run scripts/audit-barrels.ts --fix` auto-rewrites.
2. **C1 router calls** — only `navigation/NavigationHelper.tsx` (all calls now flow through the curtain seam) and `hooks/useAuthNavigation.ts`.
3. **S9 supabase** — no `supabase.*` outside `utils/supabase/*` / `services/*`.
4. **S7 hex colors** — never hardcode `'#E8590C'`; pull from `theme.colors.*` via `useAppTheme()` or a constants import.
5. **S11 console** — `logger` only.

## Arqavellum relationship

Direct-copy consumer of the public starter (sibling at `../arqavellum`, `github.com/moidotsh/arqavellum`). The shell — MobilePremium kit + showcase, audit scripts, PWA plumbing, provider stack, auth scaffolding, navigation helper, hooks — is copied file-by-file and re-synced when the shell evolves.

**Sync procedure:** copy the relevant files from `../arqavellum/`, then re-apply armandotfit's overrides — `constants/theme.ts` (ember palette + `DIALECT = 'ink'`; the structure comes from the shell, only the values and the dialect pick are ours), `constants/displayName.ts`, `constants/layout.ts` (`authGuard: true`), icon assets, `index.html` (ember theme-colors + boot plate), the domain routes in `NavigationHelper.tsx`, `navigation/routeMetadata.ts`. Copy the kit WHOLE (the showcase is the visual source of truth and demos every primitive) but wire into app routes only what this app uses. Currently synced-but-unwired: i18n harness + `LangToggle`, `MobileAbsorbBar`, `MobileAnnouncementBar`, `MobileFootnote`, `CheckBox`, `Wizard`/`MobileStepper`, `ScreenScaffold`. Shell-level bug fixes made here port back to arqavellum (public-repo discipline applies there — no consumer names in the public repo).

## Documentation maintenance

| Change you're making | Update this doc | When |
|---|---|---|
| New pattern (S/C/D/SE/T/R code) | `ARCHITECTURE.md` + new `scripts/audit-*.ts` + this file's table. | Always. |
| New `scripts/audit-*.ts` | This file's 13-audit grid, in run order. | Always. |
| Visual token change | `constants/theme.ts` (canonical) + `docs/architecture/mobile-premium-design-system.md` if kit-level. | Always. |
| New MobilePremium primitive | Design-system doc inventory + the showcase (synced with the kit). | Always. |
| New hook / utility | Folder barrel (+ showcase demo if visible output). | Always. |
| New route / push-replace helper | `NavigationHelper.tsx` (enum + hierarchy + curtain-wrapped helper) + `routeMetadata.ts` title. | Always. |
| PWA plumbing change | `docs/architecture/pwa-installability.md`; keep `index.html`, the injector, and the runtime block in sync. | Always. |
| Schema migration | Migration file header (multi-line why). The greenslate file is the whole schema — edit it only with owner sign-off (invariant 4). | Always. |
| Exercise identity/tag vocabulary change | `docs/architecture/exercise-logging-governance.md` (identity test + promotion rule live there). | Always. |

### Rules

1. **One owner per claim.** If two docs appear to own the same claim, one is canonical and the other cross-links.
2. **Navigation layers don't restate content.** `README.md` and this file's intro are navigation surfaces.
3. **Audit scripts are canonical.** Scripts win over this cheatsheet.

## Canonical docs

| Claim type | Canonical owner |
|---|---|
| Repo operating context | this file |
| Architecture constitution (47 patterns) | `ARCHITECTURE.md` |
| Project orientation | `README.md` |
| Claim-type → owner map | `docs/OWNERSHIP.md` |
| Exercise identity + logging governance (identity test, tags, promotion rule) | `docs/architecture/exercise-logging-governance.md` |
| MobilePremium design system | `docs/architecture/mobile-premium-design-system.md` |
| The active design thesis (THE BROADSHEET: the daily paper — the headline law, agate figures, the record red, the wire) | `docs/architecture/broadsheet-thesis.md` |
| PWA installability | `docs/architecture/pwa-installability.md` |
| Theme tokens / palette values | `constants/theme.ts` |
